[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
evm_version = "cancun"
fs_permissions = [{ access = "read", path = "test/data" }]

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
